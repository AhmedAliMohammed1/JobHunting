import Link from "next/link";
import { ExternalLink, ShieldCheck } from "lucide-react";
import { SectionShell } from "@/src/components/dashboard/section-shell";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;return <SectionShell eyebrow="Job detail" title="Inspect before you act." description={`Job reference ${id}. Source verification, match factors, and required answers appear here when loaded from the database.`}><div className="settings-grid"><article className="product-card"><h2>Match breakdown</h2><p>Skills, title, experience, location, employment type, workplace type, semantic similarity, and recency are scored separately.</p></article><article className="product-card"><h2><ShieldCheck/> Application safety</h2><p>No listing is submitted from this page. Open the verified source and review required facts first.</p><Link href="/search">Return to search <ExternalLink size={14}/></Link></article></div></SectionShell>}
